[project]
name = "gcrm-scout-agent"
version = "0.1.0"
requires-python = ">=3.11"
description = "LangGraph agent that scores candidate contacts for mission fit."
dependencies = [
    "langgraph>=0.2.0",
    "langchain-core>=1.3.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gcrm_scout_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
